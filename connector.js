/*
=====================================
Сonnector JS
/connector.js
🐳 Глава департамента Java–Script

Назначение:
– Единственная точка подключения JS на странице
– Загружает компоненты как ES-модули

Сутры:
1. В HTML НЕТ inline-скриптов и логики — только теги компонентов
2. Каждый компонент подключается ОДНОЙ строкой через import()
3. Компоненты сами себя регистрируют (customElements.define внутри файла)
4. Порядок import важен, если компоненты зависят друг от друга

Как добавлять компонент:
– Создать файл в /js/components/
– Подключить ОДНОЙ строкой ниже
– Использовать в HTML как <comp-name></comp-name>
===================================== */

document.addEventListener('DOMContentLoaded', async () => {
  await import('./js/components/CompFontResize.js');
  await import('./js/components/CompNoWidows.js');
  await import('./js/components/CompDynamicOpacity.js');
});
  